# 공유 규칙 · 표기·용어 관례

> `shared/` = 팀 전원 공통. 직접 고치지 말고 합의 후에만.

## 용어는 용어집으로

- 팀이 자주 쓰는 용어·약어는 각자 정의하지 말고 한 곳(예: `team_memory/atoms/concepts/`)에 모은다.
- 같은 것을 사람마다 다르게 부르면 협업 비용이 커진다.

## 파일·폴더 이름

- 날짜는 `YYYY-MM-DD` (사전순 = 시간순이 되도록).
- 개인 것은 `<이름>/` 아래, 공유 것은 `shared/` 또는 `atoms/` 아래.
